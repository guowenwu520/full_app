<?xml version="1.0" encoding="utf-8"?>
<!--
    最近动态卡片
    复用已有资源：
    @drawable/bg_realm_status_card
    @drawable/ic_core_streak
    @color/checkin_text_primary
    @color/exp_text_secondary
    @color/exp_divider
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/recentActivityCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="8dp"
    android:background="@drawable/bg_realm_status_card"
    android:clipToOutline="true"
    android:elevation="3dp"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingTop="8dp"
    android:paddingEnd="12dp"
    android:paddingBottom="8dp">

    <!-- 标题栏 -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="28dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_core_streak" />

            <TextView
                android:id="@+id/tvRecentActivityTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:fontFamily="serif"
                android:includeFontPadding="false"
                android:text="@string/recent_activity"
                android:textColor="@color/checkin_text_primary"
                android:textSize="14sp"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvViewAllActivity"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="end"
            android:drawableEnd="@drawable/ic_arrow_right_small"
            android:drawablePadding="4dp"
            android:gravity="center"
            android:paddingStart="10dp"
            android:text="@string/view_all"
            android:textColor="@color/exp_text_secondary"
            android:textSize="10sp" />
    </FrameLayout>

    <!-- 动态列表 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerRecentActivity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:nestedScrollingEnabled="false"
        android:overScrollMode="never"
        android:scrollbars="none" />

</LinearLayout>
