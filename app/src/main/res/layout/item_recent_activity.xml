<?xml version="1.0" encoding="utf-8"?>
<!-- RecyclerView 单条最近动态 -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activityItemRoot"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:background="?android:attr/selectableItemBackground"
    android:paddingStart="6dp"
    android:paddingEnd="4dp">

    <ImageView
        android:id="@+id/ivActivityIcon"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:contentDescription="@null"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_exp_reading" />

    <!-- 类型 + 内容 -->
    <LinearLayout
        android:id="@+id/groupActivityText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/tvActivityTime"
        app:layout_constraintStart_toEndOf="@id/ivActivityIcon"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvActivityType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textColor="@color/checkin_text_primary"
            android:textSize="11sp"
            tools:text="阅读打卡：" />

        <TextView
            android:id="@+id/tvActivityContent"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="3dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textColor="@color/checkin_text_primary"
            android:textSize="11sp"
            tools:text="《三体》阅读至第 126 页" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvActivityTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/exp_text_secondary"
        android:textSize="9sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="1小时前" />

    <View
        android:id="@+id/activityDivider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/exp_divider"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/groupActivityText" />

</androidx.constraintlayout.widget.ConstraintLayout>
